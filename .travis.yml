language: php

php:
    - 7.0
    - 7.1
    - 7.2
    - nightly

matrix:
    fast_finish: true
    allow_failures:
        - php: nightly

before_script:
    - if [[ $TRAVIS_PHP_VERSION = nightly* ]]; then pecl install -f xdebug-beta; fi
    - composer install
